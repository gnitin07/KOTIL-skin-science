# dependencies
node_modules/

# build output
dist/

# Large original source media (139 MB): raw machine photos, logo screenshot and
# the services screenshots. These are only inputs to scripts/*.mjs — the built
# web assets in public/ are what the site actually ships. Keep a separate backup.
raw-assets/

# logs & env
*.log
npm-debug.log*
.env
.env.*

# editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
