# dependencies
node_modules/

# build output
dist/

# NOTE: raw-assets/ is NOT ignored — it is versioned via Git LFS (see
# .gitattributes). It holds the high-res originals that scripts/*.mjs turn into
# the optimised files in public/. Run `git lfs install` after cloning.

# logs & env
*.log
npm-debug.log*
.env
.env.*

# editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
